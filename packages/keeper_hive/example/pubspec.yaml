name: keeper_hive_example
description: Examples for Keeper Hive
private: true
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  keeper:
  keeper_hive:
  hive:

dependency_overrides:
  keeper:
    path: ../../keeper
  keeper_hive:
    path: ../
  keeper_codegen:
    path: ../../keeper_codegen

dev_dependencies:
  build_runner:
  keeper_codegen: